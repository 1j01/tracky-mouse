{
  "private": true,
  "name": "tracky-mouse-website",
  "version": "2.0.0",
  "description": "Website for Tracky Mouse",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "live-server": "^1.2.1"
  },
  "scripts": {
    "start": "live-server --port=64206",
    "test": "node ../scripts/copy-with-review.cjs . ../dist deploy-patterns.js --dry-run",
    "deploy": "rm -rf ../dist && node ../scripts/copy-with-review.cjs . ../dist deploy-patterns.js && gh-pages -d ../dist"
  },
  "license": "MIT"
}
